person_fusion_node:
  ros__parameters:
    # Matching threshold between LiDAR and Camera positions (meters)
    # Sim: 0.3 m
    # Real: 0.5 m
    match_threshold: 0.3
    
    # Camera data timeout (seconds)
    # If camera verified data older than this, skip fusion
    camera_timeout: 1.0